# Разработка функциональности дистанционного заказа справок в виде электронного документа на базе личного кабинета обучающегося в МГТУ «СТАНКИН»

## 📋 Краткое описание

Сервис, который позволяет студентам дистанционно подавать заявки на получение справок и, опционально, получать их в электронном виде, избегая очередей. Микросервис предназначен для интеграции с основным приложением "Личный кабинет обучающегося" и обеспечивает полный цикл обработки заявок от создания до получения готового документа.

---

## 🛠 Стек технологий

### Frontend
- **Framework**: Angular 17 (standalone components)
- **Language**: TypeScript 5.2
- **HTTP Client**: RxJS Observables
- **Styling**: CSS3 с кастомными переменными
- **Build Tool**: Angular CLI
- **Web Server**: Nginx (для production)

### Backend
- **Framework**: ASP.NET Core 8.0
- **Language**: C#
- **Architecture Pattern**: Clean Architecture (Domain, Application, Infrastructure, WebAPI)
- **ORM**: Entity Framework Core 9.0
- **API Documentation**: Swagger/OpenAPI

### База данных
- **DBMS**: PostgreSQL 15
- **Provider**: Npgsql.EntityFrameworkCore.PostgreSQL

### Деплой/Инфраструктура
- **Containerization**: Docker
- **Orchestration**: Docker Compose (для локальной разработки)
- **Production Deployment**: Kubernetes (планируется)
- **Reverse Proxy**: Nginx (встроен во фронтенд контейнер)

### Сторонние API и интеграции
- **Аутентификация**: Внешний сервис аутентификации (для разделения ролей: админ/пользователь)
- **1С:Документооборот**: Интеграция для автоматической обработки заявок (реализована частично)

---

## 🏗 Архитектура проекта

### Архитектурный стиль

Проект реализован в стиле **Clean Architecture** с разделением на слои:

1. **Domain** - Бизнес-логика, сущности, модели, enum'ы
2. **Application** - Контракты (интерфейсы), DTO, бизнес-логика приложения
3. **Infrastructure** - Реализация репозиториев, работа с БД, внешние сервисы
4. **WebAPI** - Контроллеры, конфигурация, точка входа

Проект представляет собой **микросервис**, предназначенный для интеграции с основным приложением "Личный кабинет обучающегося" через API Gateway.

### Диаграмма взаимодействия компонентов

```
┌─────────────┐
│   Клиент    │
│  (Браузер)  │
└──────┬──────┘
       │
       │ HTTP/HTTPS
       │
┌──────▼─────────────────────────────────────────┐
│           API Gateway / Nginx                  │
│  (Маршрутизация, балансировка, аутентификация) │
└──────┬─────────────────────────────────────────┘
       │
       │ REST API
       │
┌──────▼────────────────────────────────────────┐
│      Микросервис заказа справок               │
│  ┌─────────────────────────────────────────┐  │
│  │  Frontend (Angular)                     │  │
│  │  - UI компоненты                        │  │
│  │  - Сервисы для API                      │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                             │
│  ┌──────────────▼──────────────────────────┐  │
│  │  Backend (ASP.NET Core)                 │  │
│  │  - Контроллеры (User, Admin)            │  │
│  │  - Бизнес-логика                        │  │
│  │  - Репозитории                          │  │
│  └──────────────┬──────────────────────────┘  │
└─────────────────┼─────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
┌───────▼──────┐   ┌────────▼───────┐
│  PostgreSQL  │   │  1С:Документо- │
│   Database   │   │    оборот      │
│              │   │                │
│ - Заявки     │   │ - Обработка    │
│ - Статусы    │   │ - Генерация    │
│ - Файлы      │   │   документов   │
└──────────────┘   └────────────────┘
```

### Поток данных при создании заявки

1. **Студент** → Frontend: Заполняет форму заявки
2. **Frontend** → Backend: POST `/api/user/CreateRequest`
3. **Backend** → Database: Сохраняет заявку со статусом `New`
4. **Backend** → Frontend: Возвращает подтверждение

### Поток данных при интеграции с 1С

1. **1С:Документооборот** → Backend: GET `/api/admin/admin/SubmitTo1C` (периодически)
2. **Backend** → Database: Получает заявки со статусом `New`
3. **Backend** → 1С: Возвращает список заявок для обработки
4. **1С** → Backend: POST `/api/admin/admin/UpdateFrom1C` (после обработки)
5. **Backend** → Database: Обновляет статусы заявок и сохраняет файлы

---

## 📁 Структура проекта

```
remote-ordering-of-certificates/
│
├── Back/                          # Backend приложение
│   ├── Application/               # Слой приложения
│   │   ├── Contracts/            # Интерфейсы сервисов
│   │   │   ├── IUser.cs         # Контракт для пользовательских операций
│   │   │   └── IAdmin.cs        # Контракт для административных операций
│   │   └── DTOs/                 # Data Transfer Objects
│   │       ├── User/             # DTO для пользовательских запросов
│   │       │   ├── CreateRequest/
│   │       │   ├── GetRequest/
│   │       │   └── GetRequestList/
│   │       └── Admin/            # DTO для административных запросов
│   │           ├── AdminGetRequest/
│   │           ├── AdminGetRequestList/
│   │           ├── Requests/     # DTO для отправки в 1С
│   │           └── UpdateFrom1c/ # DTO для получения обновлений от 1С
│   │
│   ├── Domain/                    # Доменный слой
│   │   ├── Entities/             # Сущности базы данных
│   │   │   ├── RequestInformation.cs  # Полная информация о заявке
│   │   │   └── StudentRequest.cs     # Связь студента с заявкой
│   │   ├── Enums/                # Перечисления
│   │   │   ├── RequestStatus.cs      # Статусы заявки (New, Processing, Completed, Rejected)
│   │   │   ├── ReceivingFormat.cs    # Формат получения (InPerson, Remotely)
│   │   │   └── OneCStatus.cs         # Статусы в 1С
│   │   └── Models/               # Модели данных
│   │       ├── Request.cs            # Модель заявки для списков
│   │       └── OneCRequest.cs       # Модель для работы с 1С
│   │
│   ├── Infrastructure/            # Слой инфраструктуры
│   │   ├── Data/                 # Работа с БД
│   │   │   └── AppDbContext.cs   # Контекст Entity Framework
│   │   ├── DependencyInjection/  # Настройка DI
│   │   │   └── ServiceContainer.cs
│   │   └── Repo/                 # Репозитории
│   │       ├── UserRepo.cs        # Реализация IUser
│   │       └── AdminRepo.cs       # Реализация IAdmin
│   │
│   ├── WebAPI/                    # Слой представления
│   │   ├── Controllers/          # API контроллеры
│   │   │   ├── User.cs           # Эндпоинты для студентов
│   │   │   └── Admin.cs          # Эндпоинты для администраторов и 1С
│   │   ├── Program.cs            # Точка входа, конфигурация
│   │   └── appsettings.json      # Конфигурация приложения
│   │
│   └── Dockerfile                 # Docker образ для бэкенда
│
├── Front/                         # Frontend приложение
│   ├── src/
│   │   ├── app/
│   │   │   ├── components/       # Переиспользуемые компоненты
│   │   │   │   └── header/       # Хэдер с логотипом и навигацией
│   │   │   ├── config/           # Конфигурация
│   │   │   │   └── api.config.ts # Настройки API endpoints
│   │   │   ├── models/           # TypeScript модели (DTO)
│   │   │   ├── pages/            # Страницы приложения
│   │   │   │   ├── home/        # Стартовая страница
│   │   │   │   ├── student/     # Страницы для студентов
│   │   │   │   │   ├── student-dashboard/
│   │   │   │   │   ├── new-request/
│   │   │   │   │   ├── requests-list/
│   │   │   │   │   └── request-detail/
│   │   │   │   ├── admin/      # Страницы для администраторов
│   │   │   │   │   ├── requests-list/
│   │   │   │   │   └── request-detail/
│   │   │   │   └── profile/    # Страница профиля (заглушка)
│   │   │   ├── services/        # Angular сервисы
│   │   │   │   ├── user.service.ts    # Сервис для работы с User API
│   │   │   │   └── admin.service.ts   # Сервис для работы с Admin API
│   │   │   ├── app.routes.ts   # Маршрутизация
│   │   │   └── app.component.ts # Корневой компонент
│   │   ├── assets/              # Статические ресурсы
│   │   │   └── images/         # Изображения (логотип)
│   │   └── styles.css          # Глобальные стили
│   ├── Dockerfile              # Docker образ для фронтенда
│   └── nginx.conf              # Конфигурация Nginx
│
├── docker-compose.yml          # Docker Compose конфигурация
├── README_DOCKER.md            # Документация по Docker
└── Документации/               # Дополнительная документация
    ├── API.md                  # Описание API
    └── Техническое задание.pdf
```

---

## ✨ Ключевые фичи и их реализация

### Для студентов (пользователей)

#### 1. Создание новой заявки на справку
**Описание**: Студент может создать заявку, указав тип справки, формат получения, свои данные.

**Реализация**:
- **Frontend**: `Front/src/app/pages/student/new-request/new-request.component.ts`
- **Backend Controller**: `Back/WebAPI/Controllers/User.cs` → `CreateRequest`
- **Backend Service**: `Back/Infrastructure/Repo/UserRepo.cs` → `CreateRequest`
- **DTO**: `Back/Application/DTOs/User/CreateRequest/CreateRequestDTO.cs`

**Логика**:
1. Пользователь заполняет форму (ФИО, курс, формат получения)
2. Frontend отправляет POST запрос на `/api/user/CreateRequest`
3. Backend сохраняет заявку в БД со статусом `New`
4. Возвращается сообщение об успешной подаче заявки

#### 2. Просмотр списка своих заявок
**Описание**: Студент видит все свои заявки с информацией о статусе и дате создания.

**Реализация**:
- **Frontend**: `Front/src/app/pages/student/requests-list/requests-list.component.ts`
- **Backend Controller**: `Back/WebAPI/Controllers/User.cs` → `GetRequestList`
- **Backend Service**: `Back/Infrastructure/Repo/UserRepo.cs` → `GetRequestList`
- **DTO**: `Back/Application/DTOs/User/GetRequestList/GetRequestListDTO.cs`

#### 3. Просмотр детальной информации о заявке
**Описание**: Студент может открыть любую заявку и увидеть полную информацию, включая статус, формат получения, путь к файлу (если готов).

**Реализация**:
- **Frontend**: `Front/src/app/pages/student/request-detail/request-detail.component.ts`
- **Backend Controller**: `Back/WebAPI/Controllers/User.cs` → `GetRequest`
- **Backend Service**: `Back/Infrastructure/Repo/UserRepo.cs` → `GetRequest`
- **DTO**: `Back/Application/DTOs/User/GetRequest/GetRequestDTO.cs`

### Для администраторов

#### 4. Поиск заявок по ID студента
**Описание**: Администратор может найти все заявки конкретного студента по его ID.

**Реализация**:
- **Frontend**: `Front/src/app/pages/admin/requests-list/requests-list.component.ts`
- **Backend Controller**: `Back/WebAPI/Controllers/Admin.cs` → `AdminGetRequestList`
- **Backend Service**: `Back/Infrastructure/Repo/AdminRepo.cs` → `AdminGetRequestList`
- **DTO**: `Back/Application/DTOs/Admin/AdminGetRequestList/AdminGetRequestListDTO.cs`

#### 5. Просмотр детальной информации о заявке (админ)
**Описание**: Администратор видит полную информацию о любой заявке.

**Реализация**:
- **Frontend**: `Front/src/app/pages/admin/request-detail/request-detail.component.ts`
- **Backend Controller**: `Back/WebAPI/Controllers/Admin.cs` → `AdminGetRequest`
- **Backend Service**: `Back/Infrastructure/Repo/AdminRepo.cs` → `AdminGetRequest`
- **DTO**: `Back/Application/DTOs/Admin/AdminGetRequest/AdminGetRequestDTO.cs`

### Интеграция с 1С:Документооборот

#### 6. Отправка заявок в 1С
**Описание**: 1С периодически запрашивает новые заявки для обработки.

**Реализация**:
- **Backend Controller**: `Back/WebAPI/Controllers/Admin.cs` → `SubmitTo1C`
- **Backend Service**: `Back/Infrastructure/Repo/AdminRepo.cs` → `SendRequestsTo1C`
- **DTO**: `Back/Application/DTOs/Admin/Requests/RequestsDTO.cs`

**Логика**:
1. 1С отправляет GET запрос на `/api/admin/admin/SubmitTo1C`
2. Backend находит все заявки со статусом `New`
3. Возвращает список заявок для обработки
4. После получения заявок, 1С меняет их статус на `Processing`

#### 7. Получение обновлений от 1С
**Описание**: После обработки заявок 1С отправляет обновленные данные (статусы, файлы).

**Реализация**:
- **Backend Controller**: `Back/WebAPI/Controllers/Admin.cs` → `UpdateFrom1C`
- **Backend Service**: `Back/Infrastructure/Repo/AdminRepo.cs` → `UpdateFrom1C`
- **DTO**: `Back/Application/DTOs/Admin/UpdateFrom1c/UpdateFrom1cDTO.cs`

**Логика**:
1. 1С отправляет POST запрос на `/api/admin/admin/UpdateFrom1C`
2. В теле запроса передается список обработанных заявок с файлами
3. Backend обновляет статусы заявок (на `Completed` или `Rejected`)
4. Сохраняет файлы справок в файловую систему
5. Обновляет пути к файлам в базе данных

---

## 📖 Пользовательские сценарии (Use Cases)

### Сценарий 1: Студент создает заявку на справку

**Актор**: Студент

**Предусловия**: Студент авторизован в системе

**Основной поток**:

1. Студент открывает приложение и выбирает "Я обучающийся"
2. На странице дашборда нажимает "Заказать новую справку"
3. Заполняет форму:
   - Выбирает тип справки (по умолчанию "Справка об обучении")
   - Выбирает формат получения: "Очно" или "Электронно"
   - Вводит свой ID студента
   - Вводит ФИО
   - Выбирает курс обучения
4. Нажимает кнопку "Отправить заявку"
5. Система сохраняет заявку в БД со статусом `New`
6. Система отображает сообщение об успешной подаче заявки
7. Студент автоматически перенаправляется на страницу со списком своих заявок

**Альтернативный поток** (ошибка валидации):
- Если не все поля заполнены, система отображает сообщение об ошибке
- Студент может исправить данные и отправить заявку снова

**Постусловия**: Заявка создана и находится в статусе `New`, ожидает обработки

---

### Сценарий 2: Обработка заявки через 1С и получение справки

**Акторы**: 1С:Документооборот, Студент

**Предусловия**: В системе есть заявки со статусом `New`

**Основной поток**:

1. **Интеграция с 1С**:
   - 1С периодически (например, каждые 5 минут) отправляет GET запрос на `/api/admin/admin/SubmitTo1C`
   - Backend возвращает список всех заявок со статусом `New`
   - 1С получает заявки и начинает их обработку
   - Backend автоматически меняет статус заявок на `Processing`

2. **Обработка в 1С**:
   - 1С генерирует справки на основе данных из заявок
   - Создает электронные документы (PDF)
   - Подготавливает данные для отправки обратно

3. **Обновление статусов**:
   - 1С отправляет POST запрос на `/api/admin/admin/UpdateFrom1C`
   - В теле запроса передаются обработанные заявки с файлами справок
   - Backend обновляет статусы заявок:
     - На `Completed` - если справка успешно создана
     - На `Rejected` - если возникла ошибка при создании
   - Backend сохраняет файлы справок в файловую систему
   - Обновляет пути к файлам в базе данных

4. **Просмотр результата студентом**:
   - Студент открывает список своих заявок
   - Видит, что статус заявки изменился на "Завершена"
   - Открывает детальную информацию о заявке
   - Видит путь к файлу справки (если формат получения "Электронно")
   - Может скачать справку (функционал скачивания реализуется отдельно)

**Альтернативный поток** (ошибка обработки):
- Если 1С не может обработать заявку, она возвращается со статусом `Rejected`
- Студент видит статус "Отклонена" в списке заявок
- Может создать новую заявку или обратиться в поддержку

**Постусловия**: Заявка обработана, справка создана и сохранена в системе

---

## 🚀 Инструкция по локальному запуску (Getting Started)

### Требования

- **.NET SDK 8.0** или выше
- **Node.js 18** или выше
- **npm** или **yarn**
- **PostgreSQL 15** (или Docker для запуска через Docker Compose)
- **Docker** и **Docker Compose** (опционально, для контейнеризации)

### Вариант 1: Запуск через Docker Compose (рекомендуется)

1. **Клонируйте репозиторий** (если еще не сделали):
```bash
git clone <repository-url>
cd remote-ordering-of-certificates
```

2. **Запустите все сервисы одной командой**:
```bash
docker-compose up -d
```

3. **Проверьте статус контейнеров**:
```bash
docker-compose ps
```

4. **Откройте приложение в браузере**:
   - Фронтенд: http://localhost:4200
   - Бэкенд API: http://localhost:5066
   - Swagger UI: http://localhost:5066/swagger

5. **Остановка приложения**:
```bash
docker-compose down
```

Подробная документация по Docker: см. [README_DOCKER.md](README_DOCKER.md)

### Вариант 2: Локальный запуск без Docker

#### Настройка базы данных PostgreSQL

1. **Установите PostgreSQL 15** (если еще не установлен)

2. **Создайте базу данных**:
```sql
CREATE DATABASE db;
CREATE USER postgres WITH PASSWORD 'pass';
GRANT ALL PRIVILEGES ON DATABASE db TO postgres;
```

3. **Обновите строку подключения** в `Back/WebAPI/appsettings.json`:
```json
{
  "ConnectionStrings": {
    "Default": "Host=localhost;Database=db;Username=postgres;Password=pass;"
  }
}
```

#### Запуск бэкенда

1. **Перейдите в папку Back**:
```bash
cd Back
```

2. **Восстановите зависимости**:
```bash
dotnet restore
```

3. **Примените миграции** (если они есть):
```bash
dotnet ef database update --project Infrastructure --startup-project WebAPI
```

4. **Запустите приложение**:
```bash
cd WebAPI
dotnet run
```

Бэкенд будет доступен по адресу: http://localhost:5066

#### Запуск фронтенда

1. **Откройте новый терминал и перейдите в папку Front**:
```bash
cd Front
```

2. **Установите зависимости**:
```bash
npm install
```

3. **Запустите dev-сервер**:
```bash
npm start
```

Фронтенд будет доступен по адресу: http://localhost:4200

**Примечание**: Убедитесь, что бэкенд запущен перед запуском фронтенда, так как фронтенд делает запросы к API.

### Настройка CORS

Если возникают проблемы с CORS при локальной разработке, убедитесь, что в `Back/WebAPI/Program.cs` настроен CORS для `http://localhost:4200`.

---

## 📡 Описание API эндпоинтов

### User API (для студентов)

| Метод | Endpoint | Описание | DTO Request | DTO Response |
|-------|----------|----------|-------------|--------------|
| POST | `/api/user/CreateRequest` | Создание новой заявки на справку | `CreateRequestDTO` | `CreateRequestResponse` |
| GET | `/api/user/GetRequest` | Получение детальной информации о заявке | `GetRequestDTO` | `GetRequestResponse` |
| GET | `/api/user/GetRequestList` | Получение списка заявок студента | `GetRequestListDTO` | `GetRequestListResponse` |

#### CreateRequestDTO
```typescript
{
  studentId: number;        // ID студента
  fullName: string;         // ФИО студента
  course: number;            // Курс обучения (1-6)
  receivingFormat: string;  // Формат получения: "InPerson" | "Remotely"
}
```

#### GetRequestDTO
```typescript
{
  requestID: string;  // GUID заявки
}
```

#### GetRequestListDTO
```typescript
{
  studentId: number;  // ID студента
}
```

#### CreateRequestResponse
```typescript
{
  message: string;  // Сообщение об успешной подаче заявки
}
```

#### GetRequestResponse
```typescript
{
  fullRequestInformation: {
    id: string;                    // GUID заявки
    name: string;                  // ФИО студента
    fullRequestStatus: string;     // Статус: "New" | "Processing" | "Completed" | "Rejected"
    date: string;                  // Дата создания (ISO 8601)
    filePath: string;              // Путь к файлу справки (если готов)
    receivingFormat: string;       // Формат получения: "InPerson" | "Remotely"
  }
}
```

#### GetRequestListResponse
```typescript
{
  listOfRequests: [
    {
      id: string;                  // GUID заявки
      requestStatus: string;       // Статус: "New" | "Processing" | "Completed" | "Rejected"
      date: string;                // Дата создания (ISO 8601)
      receivingFormat: string;     // Формат получения: "InPerson" | "Remotely"
    }
  ]
}
```

---

### Admin API (для администраторов и 1С)

| Метод | Endpoint | Описание | DTO Request | DTO Response |
|-------|----------|----------|-------------|--------------|
| GET | `/api/admin/admin/GetRequest` | Получение детальной информации о заявке (админ) | `AdminGetRequestDTO` | `AdminGetRequestResponse` |
| GET | `/api/admin/admin/GetRequestList` | Получение списка заявок по ID студента | `AdminGetRequestListDTO` | `AdminGetRequestListResponse` |
| GET | `/api/admin/admin/SubmitTo1C` | Получение новых заявок для обработки в 1С | `RequestsDTO` (пустой) | `RequestsResponse` |
| POST | `/api/admin/admin/UpdateFrom1C` | Обновление статусов заявок после обработки в 1С | `UpdateFrom1cDTO` | `UpdateFrom1cResponse` |

#### AdminGetRequestDTO
```typescript
{
  requestID: string;  // GUID заявки
}
```

#### AdminGetRequestListDTO
```typescript
{
  studentId: number;  // ID студента
}
```

#### AdminGetRequestResponse
```typescript
{
  fullRequestInformation: {
    id: string;
    name: string;
    fullRequestStatus: string;
    date: string;
    filePath: string;
    receivingFormat: string;
  }
}
```

#### AdminGetRequestListResponse
```typescript
{
  listOfRequests: [
    {
      id: string;
      requestStatus: string;
      date: string;
      receivingFormat: string;
    }
  ]
}
```

#### RequestsDTO
```typescript
{}  // Пустой объект, параметры не требуются
```

#### RequestsResponse
```typescript
{
  requests: [
    {
      id: string;                    // GUID заявки
      name: string;                  // ФИО студента
      fullRequestStatus: string;     // Статус заявки
      date: string;                  // Дата создания
      filePath: string;              // Путь к файлу
      receivingFormat: string;       // Формат получения
    }
  ]
}
```

#### UpdateFrom1cDTO
```typescript
{
  requests: [
    {
      id: string;           // GUID заявки
      requestFile?: File;   // Файл справки (опционально)
      // Дополнительные поля статуса обработки
    }
  ]
}
```

#### UpdateFrom1cResponse
```typescript
{
  message: string;  // Сообщение об успешном обновлении
}
```

---

## 🔐 Безопасность и аутентификация

**Примечание**: В текущей версии аутентификация реализована частично. В production версии необходимо:

1. Интегрировать внешний сервис аутентификации
2. Реализовать JWT токены для авторизации
3. Добавить проверку ролей (студент/администратор) на уровне контроллеров
4. Настроить HTTPS для всех соединений
5. Реализовать rate limiting для защиты от DDoS

---

## 🧪 Тестирование

### Тестирование API через Swagger

1. Запустите бэкенд
2. Откройте http://localhost:5066/swagger
3. Используйте встроенный UI для тестирования эндпоинтов

### Тестирование фронтенда

```bash
cd Front
npm test
```

---

## 📝 Дополнительная документация

- [README_DOCKER.md](README_DOCKER.md) - Подробная документация по Docker
- [Документации/API.md](Документации/API.md) - Дополнительное описание API
- [Front/README.md](Front/README.md) - Документация по фронтенду
- [Front/CORS_SETUP.md](Front/CORS_SETUP.md) - Настройка CORS

---

## 🤝 Вклад в проект

При внесении изменений в проект:

1. Следуйте архитектурным принципам Clean Architecture
2. Обновляйте документацию при изменении API
3. Тестируйте изменения локально перед коммитом
4. Используйте осмысленные сообщения коммитов

---

## 📄 Лицензия

[Указать лицензию проекта]

---

## 👥 Контакты

[Указать контакты команды разработки]

---

**Версия документа**: 1.0  
**Последнее обновление**: 2025

